# Railway.toml - Railway Configuration
# This file tells Railway how to build and deploy the Python Cortex backend

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn cortex:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
