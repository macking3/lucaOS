# Railway-optimized requirements.txt
# Lightweight dependencies for cloud deployment
# Heavy ML dependencies (torch, transformers) removed for Railway

# Core FastAPI
fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.2.1
pydantic==2.12.5

# Google AI
google-generativeai==0.8.6
google-genai==1.56.0

# Networking
websockets==15.0.1
requests==2.32.5
aiohttp==3.13.2

# Utilities
networkx==3.6.1
numpy==1.26.4
pillow==12.0.0

# LightRAG for knowledge graph and RAG features
lightrag-hku==1.4.9.10

# Flask for compatibility (if needed)
flask-cors==6.0.2

# Note: This is the LIGHTWEIGHT version for Railway cloud deployment
# For local development with full features, use: pip install -r requirements.local.txt
